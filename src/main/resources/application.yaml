server:
  port: 8083

spring:
  application:
    name: ms-inscripcion

  datasource:
    url: jdbc:mysql://localhost:3306/gimnasio_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  file:
    name: logs/ms-inscripcion.log
  level:
    root: INFO
    cl.plataforma_gimnasio: INFO